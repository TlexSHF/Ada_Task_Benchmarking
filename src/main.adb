with MyTasks; use MyTasks;
with MicroBit.Console; use MicroBit.Console;

procedure Main with Priority => 0 is

begin
   loop
      -- Empty main loop to keep the program from rebooting

      -- Put_Line("Hello from main");
      -- delay(1.0);
      null;
   end loop;
end Main;
